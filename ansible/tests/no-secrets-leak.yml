- name: Test - stacks role does not leak secrets
  hosts: localhost
  gather_facts: false
  vars:
    stacks_skip: []
    stacks_src_dir: "{{ lookup('env', 'RUNNER_TEMP') }}/test-stacks"
    stacks: {}
  roles:
    - stacks

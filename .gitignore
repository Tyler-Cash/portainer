# Decrypted secrets (if you ever decrypt locally)
*.dec
*.decrypted
*.plaintext

# Age private keys (should NEVER be committed)
keys.txt
*.age-key

# Editor/OS junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# Env overrides (local-only tweaks)
.env.local
.env.*.local

.claude/
!.claude/settings.json

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars
terraform/.terraform.lock.hcl

# Node dependencies
stacks/zipline/transcoder/node_modules/
stacks/ts-clipper/web/node_modules/
stacks/ts-clipper/web/.next/
